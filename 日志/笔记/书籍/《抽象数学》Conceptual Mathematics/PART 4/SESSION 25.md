# Labelings and products of graphs

# 图表的标签和积

## 关键词





## 摘记

**01 对积的深入理解**

> I think I understand what a product is, but I don’t quite understand  what the X is that appears in the diagram
>
> 我想我明白什么是积，但我不太明白图中出现的 X 是什么
>
> Well, in the definition of product of B1 and B2 in e, there are infinitely many  conditions the projections have to satisfy: one for each object X and each pair of  maps f1 and f2.
>
> 好的，在 e 中 B1 和 B2 的乘积的定义中，投影必须满足无限多个条件：每个对象 X 和每对映射 f1 和 f2 。

> Fortunately, in some categories to show that P (with its p] and p2) is a product, we  need only compare it with a few objects. For example, in the category of sets it suffices to test P against the terminal object; and in the category of graphs you were  asked in Session 22 to prove that we need only test P against ‘the naked dot’ X =  and ‘the arrow’ X = | ♦ —»-» |. Once we have tested P against these few, so that we  know it is a product, we can take advantage of the fact that the universal property  holds for each and every object X and every pair of maps j\ ,f2. We wrote this briefly as
>
> 幸运的是，在某些类别中，要证明 P（及其 p] 和 p2）是一个乘积，我们只需将其与几个对象进行比较即可。例如，在集合范畴中，只需针对终端对象测试 P 就足够了；在第 22 节的图表类别中，您被要求证明我们只需要针对“裸点” $\bullet$ 和“箭头” $\bullet \to \bullet$ 来测试 P。一旦我们针对这几个对象测试了 P，以便我们知道它是一个产品，我们就可以利用这样的事实：通用属性适用于每个对象 X 和每对映射 j\,f2。我们简单地将其写为
> $$
> \frac{X \to B_1 \times B_2}{X \to B_1, X \to B_2}
> $$
> meaning that to specify a morphism from X to the product is the same as to specify two morphisms, one from X to each of the factors.
>
> 这意味着指定从 X 到乘积的态射与指定两个态射相同，一个从 X 到每个因子。



**02 通过标签检测图的结构（Detecting the structure of a graph by means of labelings）**

> To continue with the example of the category of graphs, we will see that the mere fact that the product of ‘the arrow’ A with any other graph Y has a map to A (the  projection p{ : A x Y —> A) gives us some information about the structure of  A x Y.
>
> 继续图类别的示例，**我们将看到“箭头”A 与任何其他图 Y 的乘积具有到 A 的映射**（投影 p{ : A x Y —> A）这一事实为我们提供了有关 A x Y 结构的一些信息。



**03 计算图形 A x Y（Calculating the graphs A x Y）**

> As we saw earlier in this session, a graph of the form Ax Y has a structure similar to  that of the graph X pictured earlier. In other words, the dots of A x Y are divided  into two sorts so that in one of them there are no targets and in the other there are no sources. Furthermore,
>
> 正如我们在本课程前面看到的，Ax Y 形式的图具有与前面所示的图 X 类似的结构。换句话说，A x Y 的点被分为两种，其中一种没有目标，另一种没有源。此外，



## 思路

