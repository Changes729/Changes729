# The category of sets

# 范畴论中的集合

## 关键词

- 有限集（finite sets）
- 域（domain）
- 陪域（codomain）
- 自同态（endomap）
- 恒等映射（identity map）：$1_A, idf(a)$



## 摘记

01 范畴中的集合（sets）与映射（maps）

> Before giving a precise definition of ‘category’, we should become familiar with one  example, **the category of finite sets and maps.**



02 单位元？

> Definition: A point of a set X is a map 1—>X.  
>
> (If A is some familiar set, a map from A to X is called an ‘A-element’ of X; thus ‘1elements’ are points.) Since a point is a map, we can compose it with another map,  and get a point again. Here is an example:



## 思路

**01 映射**

通过人名 + 食物的方式非常好的说明了什么是集合之间的映射关系。